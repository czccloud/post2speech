# regression/ — 负样本库

存放"AI 写得不口语"的典型 bad case。每改一版 SKILL.md，把这里的用例全部跑一遍回归，防止修新问题改坏旧场景（见根目录 `README.md`）。

- 用例放在 `cases/` 下，命名：`NNN-简短描述.md`（NNN 从 001 递增，不跳号不复用）
- 内容格式：按 `TEMPLATE.md` 填写
- 已入库的用例不删不改；规则变更导致某个"期望输出"本身过时（极少见），在 `CHANGELOG.md` 说明并在用例顶部加作废标注，不直接改原文
