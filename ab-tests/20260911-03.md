# A/B 盲测 20260911-03

- 日期：2026-09-11
- 测试目的：验证 Kimi K3 规则文件 v1.2 → v1.4 的变更（对应 CHANGELOG 条目 cal-20260911-01 口语规则 2"因果收口别裸接"、cal-20260911-02 通用硬规则 9"叙事描写的雅动词换直白说法"）
- 输入来源：`corpus/tech.md` 第 1 段（对照样本：两条新规则在此输入上均不触发）
- 对比版本：版本甲 = `references/kimi-k3.md` v1.2（变更前，提交 6f10b93）；版本乙 = v1.4（变更后，提交 377ff64）
- 生成条件：同一生成会话、同一输入和任务要求，分别加载对应版本规则文件。统一任务要求为"把原文改写成适合自然朗读的中文，保留原文观点与信息，不增删事实，只输出改写结果"
- A/B 分配：已打乱并封存，盲选后补录。封存承诺 SHA-256：066fda8f5ca5bf76aa99d8878875a3f9da3dc4caec53cf4bbf055f28c81c6cea

## 测试原文

> 微服务架构的核心理念在于将单体应用拆分为一组松耦合、可独立部署的服务单元。每个服务围绕特定业务能力构建，通过轻量级通信机制（通常是 HTTP/REST 或消息队列）进行协作。这种架构风格使得团队能够独立开发、部署和扩展各自的服务，从而显著提升组织的交付效率。然而，微服务并非银弹：分布式系统固有的复杂性——包括网络延迟、数据一致性保障、服务治理与可观测性建设——都需要团队具备相应的工程能力储备。因此，在决定是否采用微服务架构时，应当充分评估业务规模、团队成熟度与运维体系的承载能力，避免为追求架构先进性而引入不必要的复杂度。

## 输出 A

> 微服务架构的核心理念，说白了就是把单体应用拆成一组小服务，松耦合，每个都能独立部署。每个服务呢，围着一项特定的业务能力构建，服务和服务之间用轻量的机制协作，通常是 HTTP 接口，或者消息队列。这么做，各个团队就能独立开发、部署、扩展自己的服务，整个组织的交付效率会高不少。不过微服务它不是万能的，分布式系统固有的复杂性，一个都跑不了。比如说网络，网络有延迟、会抖动。比如说数据，数据一致性得自己保障。还有服务治理、可观测性建设，都需要团队有相应的工程能力储备。所以我的建议是，决定上不上微服务之前，先评估清楚几件事。业务规模多大了？团队成不成熟？运维体系承载得了吗？千万别为了追求架构的先进性，引进来一堆没必要的复杂度。

## 输出 B

> 微服务这套架构，核心思路就是把一个单体应用，拆成一组松耦合的小服务，每个服务都能独立部署。拆法呢，是每个服务围着一项具体的业务能力来做，服务之间用很轻量的方式协作，一般是 HTTP 接口或者消息队列。这么拆的好处是，各团队管各的，开发、部署、扩容都能自己来，整个组织的交付效率会高不少。不过呢，微服务不是万能的。分布式系统天生那些麻烦，一个都躲不掉。比如说网络，网络会抖动、会延迟。比如说数据，数据的一致性要你自己保障。服务治理、可观测性这些，也都得有工程能力撑着。所以要不要采用微服务，我的体会是先掂量几件事。业务规模多大了？团队成不成熟？运维体系扛不扛得住？别为了追求架构先进，给自己引进来一堆没必要的复杂度。

## 盲选结果（先填）

- 盲测人：用户
- 选择：无明显差别
- 理由："差不多"（盲测人原话）

## 揭晓（后填）

- A 实际对应版本：版本乙，`references/kimi-k3.md` v1.4（变更后）
- B 实际对应版本：版本甲，`references/kimi-k3.md` v1.2（变更前）
- 封存承诺验证：通过。封存明文为 `A=乙;B=甲;nonce=535d3445e17ed71db44e45ee5e6ce698`，重算 SHA-256 与头部记录的 `066fda8f5ca5bf76aa99d8878875a3f9da3dc4caec53cf4bbf055f28c81c6cea` 一致
- 结论：对照样本判"无明显差别"，符合预期（两条新规则在 tech 题材均不触发），不计胜负、计入样本数
- 后续动作：结果汇总进 `summary.md`，与 20260911-01、02 合并判定
