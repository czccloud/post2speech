---
name: chinese-polish
description: 中文表达优化。跟用户对话、汇报进展、讲解概念、回答提问、写口语化的内容（看法、随笔、讲稿）时使用——把输出整理成像说话一样自然的中文：按气息断句、话题化停顿、语气词进句中、保留毛边、像好老师那样从听众已知的内容讲起、先结论后细节、禁 AI 腔、禁互联网黑话、禁排比对仗。凡是“这个字会被用户读到”的中文输出，动笔前加载本 skill 过一遍规范。 何时使用：需要向用户交付结果、汇报进展、讲解概念、回答提问、写口语化的文章或讲稿，或觉得自己的中文开始有“AI 味”“翻译腔”“提纲腔”的时候。
---

# 中文表达优化（chinese-polish）— 路由

本 skill 的规则**按模型分文件维护**。不同模型对同一条规则的响应不同，规则必须用该模型的真实校准数据（AI 输出 vs 真人朗读的 ASR 转写）单独校准，不能跨模型套用。本文件只做分发，不含具体规则。

## 使用方式

1. 确认当前运行的模型。
2. 按下表加载 `references/` 里对应的规则文件，之后所有输出遵守**该文件**的全部规范。
3. 当前模型不在表里：默认加载 `references/kimi-k3.md`（当前活跃迭代版本），并在交付时向用户说明“该模型尚无专属规则文件，已按 Kimi K3 版本处理”。

## 模型 → 规则文件

| 模型 | 规则文件 | 状态 |
|------|----------|------|
| GLM 5.3 | `references/glm-5.3.md` | v1.0 基线（2026-09-04 从旧版 SKILL.md 迁入，未针对 GLM 5.3 校准） |
| Kimi K3 | `references/kimi-k3.md` | v1.0（2026-09-04 从同一基线迁入，后续迭代在本文件进行） |

## 迭代约定（改规则时必读）

- 规则改动只发生在**对应模型自己的文件**里，不跨模型同步。某模型上验证过的规律要用于另一模型，必须在那个模型上重新校准后单独写入。
- 每次迭代开头报版本号：模型名 + 该文件版本 + 日期（如“Kimi K3，v1.0，2026-09-04”）。
- 归因日志统一记在 `CHANGELOG.md`，条目里注明模型名和规则文件路径。
- 新增模型：在 `references/` 下新建规则文件（文件头写明从哪个基线出发），并在上表登记一行。
